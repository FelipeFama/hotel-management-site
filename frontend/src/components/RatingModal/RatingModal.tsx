import React, { FC } from "react";

type Props = {
  isOpen: boolean;
};

export const RatingModal: FC<Props> = props => {
  const { isOpen } = props;
  return (
    <section>
      <div></div>
    </section>
  );
};
