"use client";

import {
  Chart as ChartJs,
  Tooltip,
  CategoryScale,
  LinearScale,
  BarElement,
} from "chart.js";
import { FC } from "react";
import { Bar } from "react-chartjs-2";

export const Chart: FC = () => {
  return (
    <div>
      <section></section>
    </div>
  );
};
